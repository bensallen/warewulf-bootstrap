general:
  artifacts:
    - stdout.log
    - initramfs/initramfs

test:
  override:
    - ./initramfs/build.sh > stdout.log
    - ./bin/wwbootstrap -c etc/bootstrap.json -d -b initramfs/initramfs > stdout.log
